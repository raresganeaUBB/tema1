[build]
  command = "yarn build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
  # Disable blobs for local CLI deployments if not needed
  # Blobs will work automatically when deployed through Netlify dashboard

[build.environment]
  NODE_VERSION = "18"

# Redirects pentru API (dacă backend-ul este pe alt domeniu)
# Netlify va folosi rewrites din next.config.js pentru API calls

